# Kickstart Mobile

Native Android companion for EasyA Kickstart.

The app opens `https://kickstart.easya.io`, includes a native top-100 leaderboard,
and provides configurable compact/tall widgets for any cached Kickstart token.

```bash
./gradlew assembleDebug
```

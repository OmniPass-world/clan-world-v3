{
  "name": "@clan-world/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "echo 'run: pnpm convex dev'",
    "build": "tsc -b",
    "codegen": "convex codegen --typecheck disable",
    "typecheck": "tsc --noEmit",
    "test": "vitest run convex/*.test.ts",
    "lint": "echo 'lint stub'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@clan-world/shared": "workspace:*",
    "convex": "1.17.4",
    "viem": "2.48.4"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "3.2.4"
  }
}

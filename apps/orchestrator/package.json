{
  "name": "@clan-world/orchestrator",
  "private": true,
  "version": "2.1.0",
  "type": "module",
  "scripts": {
    "dev": "tsx watch --env-file=../../.env.local src/main.ts",
    "start": "tsx --env-file=../../.env.local src/main.ts",
    "build": "tsc -b",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'lint stub'",
    "clean": "rm -rf dist .turbo"
  },
  "dependencies": {
    "@clan-world/shared": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "tsx": "4.19.2",
    "typescript": "5.9.3"
  }
}

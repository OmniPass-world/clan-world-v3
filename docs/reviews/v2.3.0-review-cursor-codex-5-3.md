# ClanWorld V2.3.0 Dev Audit — cursor-codex-5-3

## Audit Metadata
- **Scope:** Full audit of current `dev` snapshot (full-repo, not PR delta)
- **Model:** `cursor-codex-5-3`
- **Date:** 2026-05-10
- **Primary branch/commit reviewed:** `dev` at `ee6a1b475c667b9eab59cbc20752fdfaf74aaa35`
- **Isolated review worktree:** `../clan-world-game-review-v2.3.0-cursor-codex-5-3`
- **Guardrail honored:** Existing reviewer docs under `docs/reviews/*` were not used as inputs for this synthesis.

## Swarm Composition
### Wave 1 (parallel broad sweep, 12 subagents)
1. Contracts correctness (diamond, settlement, heartbeat)
2. ABI parity + generated constants/enums
3. Chain client submit-order path
4. Convex indexer decode/idempotency
5. Webhook auth + secrets
6. Convex schema/public write exposure
7. Runner tick loop/liveness
8. Orchestrator integration safety
9. Agents CLI surface + path safety
10. Web frontend env/trust/error handling
11. Mobile env/signing/security surface
12. Test/lint/typecheck/CI quality gates

### Wave 2 (sequential targeted deep dives)
1. Contract event fields vs indexer ingestion mapping
2. Tick-ordering/race analysis across heartbeat -> webhook -> indexer -> runner
3. Secrets/key-custody trace across adapters/scripts/mobile
4. Adapter mode-matrix validation (`stub`/`real` interaction risks)
5. Runtime gate execution (`pnpm typecheck`, `pnpm build`, `pnpm test`, targeted package tests, full contracts tests)

### Wave 3 (adjudication)
- Deduped overlapping findings
- Re-verified contested findings with a fresh verifier pass
- Classified into **MUST FIX / SHOULD FIX / DEFER / SKIP**

## Validation Runs (Evidence)
- `pnpm typecheck` (root): **failed**
  - `apps/mobile` JSX/React type errors (e.g. `App.tsx` `ConvexProvider`, `SafeAreaView`, `Svg`, `WebView` not valid JSX component types)
- `pnpm build` (root): **failed**
  - `apps/clan-world-mobile` requires build-time env `CLAN_WORLD_MAP_URL`
- `pnpm test` (root): **failed**
  - blocked by `@clan-world/mobile` test env requirement (`CLAN_WORLD_MAP_URL`)
- `pnpm --filter @clan-world/shared test`: **failed**
  - 2 failing tests in `packages/shared/test/clanWorldAbi.test.ts`
- `pnpm --filter @clan-world/runner test`: **passed** (125 passed, 1 skipped)
- `pnpm --filter @clan-world/server test`: **passed** (27 passed)
- `pnpm --filter @clan-world/contracts test`: **failed**
  - 497 total; 495 passed, 2 failed (`ClanWorld.t.sol::test_season_transition`, `SeasonFinalizedAbi.t.sol::test_seasonFinalizedTopicMatchesThreeArgumentAbi`)

## Consolidated Triage Table

| # | File | Line | Finding | Category |
|---|---|---|---|---|
| 1 | `apps/server/convex/mock.ts` | 25-47 | `seedMockState` is a public unauthenticated mutation that writes `worldSnapshot` and `agentLogs`. | **MUST FIX** |
| 2 | `apps/server/convex/indexer.ts` | 597-601, 607-679 | `commitSnapshot` can overwrite newer state; no monotonic guard on patch updates. | **MUST FIX** |
| 3 | `apps/server/convex/crons.ts` | 19-21 | Cron `refreshSnapshot` runs without pinned block and can produce torn snapshots under chain movement. | **MUST FIX** |
| 4 | `packages/shared/test/clanWorldAbi.test.ts` | 15-33, 47-63 | ABI tuple tests are stale (`worldPaused`/`pausedAtTs` missing), currently failing. | **MUST FIX** |
| 5 | `packages/contracts/test/ClanWorld.t.sol` | 3466-3475 | `test_season_transition` currently fails (`tick freezes before season boundary: 360 != 359`). | **MUST FIX** |
| 6 | `packages/contracts/test/SeasonFinalizedAbi.t.sol` | 17-24 | `finalizeSeason()` precondition mismatch causes failing ABI-topic test (`ClanWorld: season not ended`). | **MUST FIX** |
| 7 | `apps/mobile/App.tsx` | 499+ | `@clan-world/seeker` typecheck currently fails with multiple JSX typing errors, breaking root `typecheck`. | **MUST FIX** |
| 8 | `apps/server/convex/agentLogs.ts` | 10-17 | `postLog` is a public unauthenticated write endpoint; easy DB/log spam vector. | **SHOULD FIX** |
| 9 | `apps/server/convex/kickstart.ts` | 199-223 | `watchAndRefreshToken` is public and triggers external fetch+mutations; abuse can amplify cost/load. | **SHOULD FIX** |
| 10 | `apps/server/convex/indexer.ts` | 323-329 | `chainEvents.tick` mapping misses `settledAtTick`/`settledToTick`; event denorm becomes inaccurate. | **SHOULD FIX** |
| 11 | `apps/server/convex/bulletins.ts` | 44-63 | `seedBulletin` always inserts; duplicate `(clanId, slot)` rows can accumulate and skew reads. | **SHOULD FIX** |
| 12 | `packages/shared/src/adapters/IChainClient.ts` | 4084-4087 | Transport may resolve to `http(undefined)` when RPC vars are unset; no fail-fast config guard. | **SHOULD FIX** |
| 13 | `packages/shared/src/adapters/IChainClient.ts` | 4089-4100 | Contract/lens addresses are not validated with `isAddress` before casting and usage. | **SHOULD FIX** |
| 14 | `packages/shared/src/adapters/IChainClient.ts` | 64-78 | `validateSubmitOrderPayload` receives `submitterClanId` but does not enforce caller-target consistency. | **SHOULD FIX** |
| 15 | `packages/shared/src/adapters/IChainClient.ts` | 4197-4204 | `submitOrders` falls back to `DEPLOYER_PRIVATE_KEY` when elder key path is missing (high-blast-radius fallback). | **SHOULD FIX** |
| 16 | `packages/shared/src/adapters/IConvexClient.ts` | 199-207 | Missing `CONVEX_URL` silently stubs Convex client; can deadlock runner behavior while appearing healthy. | **SHOULD FIX** |
| 17 | `apps/clan-world-mobile/app/build.gradle.kts` | 79-85 | Only `debug` buildType signing path is explicit; release signing behavior is under-specified. | **SHOULD FIX** |
| 18 | `apps/server/convex/memory.ts` | 25-36 | `getByClan` publicly exposes memory entry values; privacy model is currently open-by-default. | **DEFER** |
| 19 | `apps/server/convex/inft.ts` | 21-46 | `getInftDemoState` exposes token/transfers/memory/bulletins without auth; may violate future custody/privacy expectations. | **DEFER** |
| 20 | `packages/contracts/src/diamond/lib/LibOrderMarket.sol` | 230, 371 | Market sell execution has no slippage floor (`minOut` semantics absent); economic policy decision needed. | **DEFER** |
| 21 | `packages/runner/src/tickLoop.ts` | 88-92, 190-191 | Tick loop processes latest observed tick only; intermediate tick replay is intentionally skipped. | **DEFER** |
| 22 | `.env.template` + `apps/server/convex/crons.ts` | 31, 6-22 | `HEARTBEAT_CALLER_ENABLED` is documented but runtime control uses different flags (`CLANWORLD_USE_*`). | **DEFER** |
| 23 | `apps/mobile/src/storage.ts` | 7, 121-127 | Wallet auth token persists in plain MMKV storage; no encrypted-at-rest mobile path in this app. | **DEFER** |
| 24 | `scripts/start-heartbeat-loop.sh` | 61-64 | Heartbeat loop uses `DEPLOYER_PRIVATE_KEY`; key-role separation should be explicitly decided for ops hardening. | **DEFER** |
| 25 | `apps/server/convex/heartbeat.ts` | 131-137 | Plain string compare for bearer secret is a theoretical timing concern but not material in this context. | **SKIP / FALSE POSITIVE** |
| 26 | `apps/server/convex/indexer.ts` | 346-373 | `Whisper`/`WhisperBroadcast` indexing branch is dead against current ABI; not an active exploitable path. | **SKIP / FALSE POSITIVE** |
| 27 | `apps/clan-world-mobile/app/build.gradle.kts` | 24-37 | Build/test failure without `CLAN_WORLD_MAP_URL` is intentional fail-fast, not a code defect. | **SKIP / FALSE POSITIVE** |
| 28 | `scripts/start-heartbeat-loop.sh` | 54 | Logging `RPC_URL_PRIMARY` is operationally noisy but not inherently a secret-leak bug by itself. | **SKIP / FALSE POSITIVE** |

## Summary Stats
- **Total findings:** 28
- **MUST FIX:** 7
- **SHOULD FIX:** 10
- **DEFER:** 7
- **SKIP / FALSE POSITIVE:** 4

### Package Heatmap (finding concentration)
- `apps/server/convex/*`: 11
- `packages/shared/*`: 6
- `packages/contracts/*`: 3
- `apps/mobile/*` + `apps/clan-world-mobile/*`: 3
- `packages/runner/*`: 1
- `scripts/*`: 2
- root config/docs alignment: 2

## Recommended Next Steps

### 1) Resolve MUST FIX items first (merge-blocking order)
1. Lock down `seedMockState` (`internalMutation` or strict auth gate), then verify no anonymous state-write endpoints remain.
2. Add monotonic snapshot guard in indexer (`tick`/`block` non-regression) and pin all refresh reads to one block for cron/manual refresh.
3. Fix failing ABI tuple tests in `packages/shared/test/clanWorldAbi.test.ts`.
4. Fix the two failing Forge tests (season transition and season finalized ABI test).
5. Restore root typecheck health for `apps/mobile` JSX type errors.

### 2) Address SHOULD FIX findings by theme
- **Public write hardening:** `postLog`, `watchAndRefreshToken`, bulletin dedup/upsert strategy.
- **Indexer correctness:** enrich tick extraction fallback (`tick`, `atTick`, `openedTick`, `settledAtTick`, `settledToTick`) and keep denorm fields coherent.
- **Adapter fail-fast:** enforce RPC/address validation and avoid silent Convex stub fallback for runtime-critical paths.
- **Key hygiene:** remove/guard deployer-key fallback in `submitOrders`; define explicit elder key requirements.
- **Mobile release safety:** define explicit `release` signing/build policy in Gradle.

### 3) DEFER items (recommended GitHub issues)
- `security(server): define auth/visibility policy for memory and iNFT demo read endpoints`
- `contracts(econ): decide and document market sell slippage policy (minOut semantics)`
- `runner(gameplay): decide whether intermediate tick replay is required on snapshot jumps`
- `ops(config): reconcile HEARTBEAT_CALLER_ENABLED docs with active runtime flags`
- `mobile(security): evaluate encrypted persistence for wallet auth token in Expo app`
- `ops(keys): separate heartbeat hot key from deployer key in loop tooling`
- `server(kickstart): enforce watched-token table bounds under public action traffic`

### 4) Overall health assessment
- **Status:** **Needs work (not merge-ready)**
- Primary blockers are failing tests/typecheck and unauthenticated state mutation surfaces in Convex.
